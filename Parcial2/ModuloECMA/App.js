
import * as pentagon from './ModuloComun';

const lado = 5;
const area = pentagon.calcularArea(lado);

console.log(`El área del pentágono con lados de ${lado} unidades es de: ${area} unidades cuadradas`);
